function [ output_args ] = extract_gabor_features( varagin )
%EXTRACT_GABOR_FEATURES Summary of this function goes here
%   Detailed explanation goes here

%inputs = ParseInputs(varargin);

%% EXTRACT GABOR FEATURES (SquareEnergy, MeanAmplitude)






%% Using Gabor/ implementation

    % function PI = ParseInputs(varargin)
    % 
    % % Anonymous functions for parser
    % check_is_filterbank = @(x) strcmp(class(x), 'FilterBank') ;  % Checks that input is a filter bank
    % 
    % p = inputParser;
    % p.addRequired('FilterBank', check_is_filterbank);             % Must pass a filterbank 
    % 
    % p.parse(varargin{:});                                         % Parse the results
    % 
    % PI = p.Results;
